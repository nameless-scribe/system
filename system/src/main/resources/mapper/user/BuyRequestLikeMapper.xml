<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.system.user.mapper.BuyRequestLikeMapper">

    <insert id="insert">
        insert into buy_request_like(request_id, user_id, create_time)
        values (#{requestId}, #{userId}, now())
    </insert>

    <delete id="delete">
        delete from buy_request_like
        where request_id = #{requestId}
          and user_id = #{userId}
    </delete>

    <select id="countByReqAndUser" resultType="int">
        select count(1)
        from buy_request_like
        where request_id = #{requestId}
          and user_id = #{userId}
    </select>

</mapper>

