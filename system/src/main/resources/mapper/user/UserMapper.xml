<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.system.user.mapper.UserMapper">

    <resultMap id="UserResultMap" type="com.system.user.entity.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="role" column="role"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <select id="selectById" resultMap="UserResultMap">
        select * from user where id = #{id}
    </select>

    <select id="selectByUsername" resultMap="UserResultMap">
        select * from user where username = #{username}
    </select>

    <insert id="insert" parameterType="com.system.user.entity.User">
        insert into user(username, password, role, status)
        values (#{username}, #{password}, #{role}, #{status})
    </insert>

    <select id="selectAll" resultMap="UserResultMap">
        select * from user
    </select>

    <update id="update" parameterType="com.system.user.entity.User">
        update user
        set username = #{username},
            role     = #{role},
            status   = #{status}
        where id = #{id}
    </update>

    <update id="updateStatus">
        update user
        set status = #{status}
        where id = #{id}
    </update>

    <update id="updatePassword">
        update user
        set password = #{password}
        where id = #{id}
    </update>

    <delete id="deleteById">
        delete from user where id = #{id}
    </delete>

</mapper>

