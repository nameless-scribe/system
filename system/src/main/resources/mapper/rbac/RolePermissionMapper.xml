<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.system.rbac.mapper.RolePermissionMapper">

    <resultMap id="RolePermissionResultMap" type="com.system.rbac.entity.RolePermission">
        <id property="id" column="id"/>
        <result property="roleId" column="role_id"/>
        <result property="permissionId" column="permission_id"/>
    </resultMap>

    <select id="selectByRoleId" resultMap="RolePermissionResultMap">
        select id, role_id, permission_id
        from sys_role_permission
        where role_id = #{roleId}
    </select>

    <insert id="insert" parameterType="com.system.rbac.entity.RolePermission">
        insert into sys_role_permission(id, role_id, permission_id)
        values (#{id}, #{roleId}, #{permissionId})
    </insert>

    <delete id="deleteByRoleId">
        delete from sys_role_permission where role_id = #{roleId}
    </delete>

</mapper>

